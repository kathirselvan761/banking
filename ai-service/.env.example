# ==============================================================================
# AI Service Environment Configuration Template
# ==============================================================================

# Server Network Settings
PORT=8000
HOST=0.0.0.0
ENV=development

# Upstream Node.js Backend URL (for webhooks / notifications)
BACKEND_URL=http://localhost:5000/api

# Optional: Path to saved model checkpoints
MODEL_STORAGE_PATH=./models/weights
